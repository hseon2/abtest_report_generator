# GitHub 브랜치 보호 규칙 설정 가이드

이 문서는 `main` 브랜치를 보호하여 안전한 협업 환경을 구축하는 방법을 설명합니다.

## 목차

1. [브랜치 보호가 필요한 이유](#브랜치-보호가-필요한-이유)
2. [기본 설정](#기본-설정)
3. [권장 설정](#권장-설정)
4. [선택적 설정](#선택적-설정)

## 브랜치 보호가 필요한 이유

브랜치 보호 규칙을 설정하면:

- ✅ 실수로 `main` 브랜치에 직접 푸시하는 것을 방지
- ✅ 코드 리뷰 없이 병합되는 것을 방지
- ✅ 테스트를 통과하지 못한 코드의 배포 방지
- ✅ 프로덕션 코드의 품질 유지

## 기본 설정

### 1. 브랜치 보호 설정 페이지 접근

1. GitHub 레포지토리 페이지 방문: https://github.com/hseon2/abtest_report_generator
2. 상단의 **Settings** 탭 클릭
3. 왼쪽 메뉴에서 **Branches** 선택
4. "Branch protection rules" 섹션에서 **Add rule** 또는 **Add branch protection rule** 클릭

### 2. 보호할 브랜치 지정

- **Branch name pattern** 입력란에 `main` 입력
- 이렇게 하면 `main` 브랜치에만 규칙이 적용됩니다

## 권장 설정

다음 옵션들을 체크하는 것을 권장합니다:

### ✅ Require a pull request before merging

**목적:** 직접 푸시를 막고 Pull Request를 통해서만 병합 가능하게 설정

**설정 방법:**
1. ☑️ **Require a pull request before merging** 체크
2. 하위 옵션 설정:
   - ☑️ **Require approvals**: 최소 승인 수 설정 (권장: 1명)
   - ☑️ **Dismiss stale pull request approvals when new commits are pushed**: 새 커밋이 푸시되면 기존 승인 무효화

**효과:**
- `main` 브랜치에 직접 푸시 불가
- 최소 1명의 팀원이 코드를 리뷰하고 승인해야 병합 가능
- 리뷰 후 추가 수정이 있으면 재리뷰 필요

### ✅ Require status checks to pass before merging (선택사항)

**목적:** CI/CD 테스트를 통과해야만 병합 가능

**설정 방법:**
1. ☑️ **Require status checks to pass before merging** 체크
2. ☑️ **Require branches to be up to date before merging** 체크

**참고:** 현재 프로젝트에 CI/CD가 설정되어 있지 않으면 이 옵션은 건너뛰셔도 됩니다.

### ✅ Require conversation resolution before merging

**목적:** PR의 모든 댓글/대화가 해결되어야 병합 가능

**설정 방법:**
- ☑️ **Require conversation resolution before merging** 체크

**효과:**
- 리뷰어가 남긴 모든 코멘트에 답변하고 해결해야 병합 가능
- 코드 리뷰가 형식적으로 끝나는 것을 방지

### ✅ Include administrators

**목적:** 관리자(레포지토리 소유자)도 보호 규칙을 따르도록 설정

**설정 방법:**
- ☑️ **Include administrators** 체크

**효과:**
- 모든 팀원이 동일한 규칙을 따름
- 실수로 직접 푸시하는 것 방지

## 선택적 설정

필요에 따라 추가할 수 있는 옵션들:

### 🔒 Restrict who can push to matching branches

**목적:** 특정 사용자나 팀만 푸시 가능하도록 제한

**언제 사용:** 팀이 커지고 권한 관리가 필요할 때

### 🔒 Allow force pushes / Allow deletions

**권장:** ❌ 체크하지 않기

**이유:**
- Force push는 히스토리를 덮어쓰므로 위험
- 브랜치 삭제는 실수로 프로덕션 코드를 날릴 수 있음

## 설정 완료 후

모든 설정을 완료했다면:

1. 페이지 하단의 **Create** 또는 **Save changes** 버튼 클릭
2. 설정이 즉시 적용됩니다

## 브랜치 보호 규칙 테스트

설정이 제대로 작동하는지 테스트:

### 테스트 1: 직접 푸시 차단 확인

```bash
# main 브랜치에 직접 푸시 시도
git checkout main
echo "test" >> test.txt
git add test.txt
git commit -m "test"
git push origin main

# 결과: 푸시가 거부되어야 함
# 에러 메시지: "protected branch hook declined"
```

### 테스트 2: PR을 통한 병합 확인

```bash
# 1. 새 브랜치 생성
git checkout -b test/branch-protection
git push origin test/branch-protection

# 2. GitHub에서 PR 생성
# 3. 팀원에게 리뷰 요청
# 4. 승인 후 병합 가능 확인
```

## 추천 워크플로우

브랜치 보호 규칙이 적용된 후의 표준 워크플로우:

```
1. main에서 브랜치 생성
   ↓
2. 로컬에서 코드 작성
   ↓
3. GitHub에 푸시
   ↓
4. Pull Request 생성
   ↓
5. 팀원에게 리뷰 요청
   ↓
6. 리뷰 코멘트 반영
   ↓
7. 승인 받기 (최소 1명)
   ↓
8. 모든 대화 해결
   ↓
9. Merge 버튼 활성화
   ↓
10. main 브랜치에 병합
   ↓
11. Render 자동 배포
```

## 기본 설정 요약 (소규모 팀)

빠르게 시작하고 싶다면 다음 3가지만 체크하세요:

1. ☑️ **Require a pull request before merging**
   - Require approvals: 1
2. ☑️ **Require conversation resolution before merging**
3. ☑️ **Include administrators**

이 설정만으로도 실수로 인한 배포 사고를 대부분 방지할 수 있습니다.

## 고급 설정 예시 (대규모 팀)

팀이 크고 엄격한 코드 품질 관리가 필요한 경우:

1. ☑️ Require a pull request before merging
   - Require approvals: 2명 이상
   - Dismiss stale pull request approvals
2. ☑️ Require status checks to pass before merging
   - CI/CD 파이프라인 설정
   - 린트, 테스트 자동 실행
3. ☑️ Require conversation resolution before merging
4. ☑️ Require linear history (squash merge 또는 rebase merge만 허용)
5. ☑️ Include administrators
6. ☑️ Restrict who can push to matching branches
   - 특정 팀만 권한 부여

## 문제 해결

### Q: 브랜치 보호 규칙을 설정할 수 없어요

**A:** Settings 탭이 보이지 않는다면:
- 레포지토리 소유자가 아니거나 Admin 권한이 없는 것입니다
- 소유자에게 Admin 권한을 요청하세요

### Q: 급하게 핫픽스를 배포해야 하는데 리뷰어가 없어요

**A:** 두 가지 방법:
1. (권장) 브랜치 보호 규칙에서 일시적으로 "Include administrators" 해제
2. (비권장) 브랜치 보호 규칙 전체 비활성화 → 배포 → 다시 활성화

### Q: PR 승인을 받았는데 Merge 버튼이 비활성화되어 있어요

**A:** 확인 사항:
1. 모든 대화가 해결되었는지 (Require conversation resolution 설정 시)
2. 브랜치가 최신 상태인지 (Require branches to be up to date 설정 시)
3. CI/CD 테스트가 통과했는지 (status checks 설정 시)

## 참고 자료

- [GitHub Docs - About protected branches](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches)
- [GitHub Docs - Managing a branch protection rule](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/managing-a-branch-protection-rule)

## 추가 도움이 필요하신가요?

브랜치 보호 설정에 대해 더 궁금한 점이 있다면:
- GitHub Discussions 활용
- 팀 채널에서 문의
- GitHub Support 문의

---

**마지막 업데이트:** 2026-01-12

